import 'package:flutter/material.dart';

class StickerViewPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("ステッカー表示")),
      body: Center(child: Text("設定ページです")),
    );
  }
}
